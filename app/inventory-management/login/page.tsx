'use client';
import { FormEvent, useState } from 'react';
import '../inventory.css';
export default function LoginPage() { const [error, setError] = useState(''); const [loading, setLoading] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setLoading(true); setError(''); const data = new FormData(event.currentTarget); const response = await fetch('/api/inventory/auth/login', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ username: data.get('username'), password: data.get('password') }) }); setLoading(false); if (!response.ok) { setError((await response.json()).error || 'Unable to sign in.'); return; } window.location.assign('/inventory-management'); }
  return <main className="im-login"><form className="im-login-card" onSubmit={submit}><p className="im-kicker">MRJ BEST BAKERY</p><h1>Inventory Management</h1><p>Sign in to manage purchases, production, sales and finances.</p><label>Username<input name="username" autoComplete="username" required /></label><label>Password<input name="password" type="password" autoComplete="current-password" required /></label>{error && <p className="im-error">{error}</p>}<button disabled={loading}>{loading ? 'Signing in…' : 'Sign in'} →</button><small>Initial access is restricted to the bakery administrator.</small></form></main>; }
